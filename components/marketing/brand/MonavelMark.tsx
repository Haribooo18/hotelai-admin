/**
 * @deprecated Import from `@/components/brand` — kept as a thin re-export
 * so existing marketing imports resolve to the official mark asset.
 */
export { MonavelMark } from "@/components/brand";
